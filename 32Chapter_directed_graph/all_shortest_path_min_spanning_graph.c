#include <stdlib.h>
#include <stdio.h>

/* Messed up The shorest path is in the file created right before I plan on updating it. */
 listbfs()
 {
	int k; 
	queueint(); 
	for (k = 1; k <= V; k++)
		val[k] = 0; 
	for (k = 1; k <= V; k++)
		if (val[k] == 0)
			visit(k); 
 }

listSHP()
{
	int y; 
	int k; 
	int x; 
	int V; 
	queueint(); 
}
